import {makeStyles} from '@material-ui/styles';

const styles = makeStyles({
    container: {
        height: 100,
        marginBottom: 20
    },
    image: {
        overflow: 'hidden',
        width: 250,
        cursor: 'pointer',
        position: 'absolute',
        top: 20
    }
});

export default styles;
